// apiUrl must be set or API requests will go to the current origin (e.g. localhost:4200).
// Set API_URL in Render Environment for production, or edit here for local dev.
export const environment = {
  production: false,
  apiUrl: 'https://segulah-api.onrender.com',
  defaultReferralCode: 'REF000000',
};
